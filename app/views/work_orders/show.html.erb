<%= link_to 'Voltar', pending_work_orders_path %>
<h1>Detalhes da ordem de serviço</h1>
  <dl>
    <dt>Endereço Destino:</dt>
    <dd><%= @work_order.full_warehouse_address %>
    <dt>Endereço de retirada:</dt>
    <dd><%= @work_order.full_customer_address %>
    <dt>Dados do cliente:</dt>
    <dd>Nome:<%= @work_order.customer_name %></dd>
    <dd>CPF:<%= @work_order.customer_cpf %></dd>
    <dd>Telefone:<%= @work_order.customer_phone_numer %></dd>
    <dt>Data de Início:</dt>
    <dd><%= I18n.l(@work_order.departure_date) %> </dd>
    <dt>Data prevista de entrega:</dt>
    <dd><%= I18n.l(@work_order.shipping_expected_date) %> </dd>
    <dt>Prazo:</dt>
    <% if (@work_order.departure_date - @work_order.shipping_expected_date) == 0 %>
    <dd><%= "Chegará Hoje" %> </dd>
    <% else %>
    <dd><%= (@work_order.shipping_expected_date - @work_order.departure_date).to_i %> dias</dd>
    <% end %>
    <dt>Valor total:</dt>
    <dd><%= number_to_currency(@work_order.total_price, :unit => "R$ ", :separator => ",", :delimiter => ".") %> </dd>
  <dl>